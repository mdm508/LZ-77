/*cody.hpp
 *cody is an encode. His primary roles are two
    *keep track of information relevant to encoding
    *preform writting
    *get information from the search buffer
    *keep the search buffer updated with lated information
*/

//INFORMATION MANIPULATION
//convert offset so its relative to front of the window
//convert LA number to an int


//WRITING TO FILE
//write string to file


//INPUT STREAM BOOKEEPING
//low: position (what is smallest index on input stream 
//  thats still in in the window)
//lab: index where in the input stream is current lab
//end of the input bufer 
//size of the input ubffer 


//KEEP SEARCH BUFFER UPDATED
//when the buffer is full, kick out oldest guys
//add occurences of patterns to the buffer 
